pre-commit:
  parallel: true
  commands:
    rust-lint:
      tags: Rust auto-formatter
      glob: "backend/*.{rs}"
      run: |
        cd backend
        cargo fmt --check || (cargo fmt && git add --all)